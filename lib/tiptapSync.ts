// Stub for TipTap editor sync utilities
// TODO: Implement proper conversion between policy JSON and HTML

export function policyJsonToHtml(json: any): string {
  return "";
}

export function htmlToPolicyJson(html: string, initialData?: any): any {
  return initialData || {};
}
